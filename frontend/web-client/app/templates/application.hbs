<nav class="navbar navbar-light bg-light">
  <a class="navbar-brand" href="#/meals/list">Toptal Calorie Tracker</a>
  {{!-- <button class="btn btn-outline-danger my-2 my-sm-0">Search</button> --}}
  {{#if isAdmin}}
    <span>ADMIN</span>
  {{else if isUserManager}}
    <span>USER MGR</span>
  {{/if}}
  {{#if session.isAuthenticated}}
    {{#link-to 'logout' class="btn"}}Logout{{/link-to}}
  {{else}}
    {{#link-to 'login' class="btn"}}Login{{/link-to}}
  {{/if}}
</nav>
{{outlet}}